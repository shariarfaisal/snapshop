import { CreateNewStore } from "@/components/stores/create-new-store";

const StorSetup = () => {
  return (
    <div className="flex min-h-screen items-center justify-center p-4">
      <CreateNewStore />
    </div>
  );
};

export default StorSetup;
